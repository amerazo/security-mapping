<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THREAT DETAILS</title>
    <!-- BOOTSTRAP CSS IMPORT -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h2 class="threat-name"><%= threat.name %></h2>
        <div class="card">
            <div class="card-body">
                <p><strong>Description:</strong> <%= threat.description %></p>
                <p><strong>URL:</strong> <%= threat.url %></p>
                <p><strong>Created:</strong> <%= threat.created.toDateString() %></p>
                <p><strong>Last Modified:</strong> <%= threat.lastModified.toDateString() %></p>
                <p><strong>Version:</strong> <%= threat.version %></p>
                <p><strong>Tactics:</strong> <%= threat.tactics %></p>
                <p><strong>Detection:</strong> <%= threat.detection %></p>
                <p><strong>Platforms:</strong> <%= threat.platforms %></p>
                <p><strong>Tactic Type:</strong> <%= threat.tacticType %></p>
                <p><strong>MTC ID:</strong> <%= threat.MTCID %></p>
                <p><strong>Relationship Citations:</strong> <%= threat.relationshipCitations %></p>
            </div>
        </div>
        <!-- EDIT & DELETE BUTTONS -->
        <a href="/threats/<%= threat._id %>/edit" class="btn btn-primary">Edit Threat</a>
        <form method="POST" action="/threats/<%= threat._id %>?_method=DELETE" class="delete-form">
            <button type="submit" class="btn btn-danger">Delete Threat</button>
        </form>
    </div>
</body>
</html>


